"""Orchestrators for the churn prediction.

Orchestrates the end-to-end workflow by combining
data, features, and model modules into cohesive
training and prediction pipelines.

Modules
--------
training_pipeline
    Defines the training pipeline, including data ingestion, feature
    engineering, model training, and evaluation steps.
prediction_pipeline
    Defines the prediction pipeline, including data ingestion, feature
    engineering, model inference, and result formatting steps.
"""
