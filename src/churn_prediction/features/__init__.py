"""Preprocessing Package.

This package provides a unified interface for both training and inference
data preprocessing pipelines within the churn-prediction project. It
consolidates the logic for dataset splitting, encoding, and type coercion.

Modules
-------
train_preprocessing
    Contains the preprocessing pipeline for training data, including dataset splitting,
    encoding, and type coercion.
inference_preprocessing
    Contains the preprocessing pipeline for inference data, including dataset splitting,
    encoding, and type coercion.
"""
